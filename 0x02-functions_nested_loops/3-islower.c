#include "main.h"
#include <stdio.h>
#include <ctype.h>

/**
 * int _islower(int c) - checks for lowercase character.
 *
*/

int _islower(int c)
{

	if (_islower(c) != 0)
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
