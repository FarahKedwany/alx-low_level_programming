#include "main.h"
#include <stdio.h>
#include <stdlib.h>

/**
 * _abs - computes the absolute value of an integer.
 *
 * @r: to be declared
 *
 * Return: r.
*/

int _abs(int r)
{
	int r;

	if (r >= 0)
		return (r);

	else if (r < 0)
		return (-r);

}
